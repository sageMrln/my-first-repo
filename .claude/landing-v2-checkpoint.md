# MRLN landing v2 — checkpoint (Opus 5 session, 2026-09-11)

Branch `raphael/landing-dd9eb47502c8`, worktree `RaphaelMrlnWork\dd9eb47502c8`.
Source of truth for the page is the assembled `landing.html`; it is generated by
`scratchpad/v2/build.py` from `template.html` + `scene.js` + three r170 +
the previous file's translated blocks (fonts, lightbox, i18n, storage→footer).

## What was delivered
"Daylight glass": white + ice-blue, one accent #2f6bd8, Cabin only. One fixed
three.js scene: brand crystal hero → opens into five glass screens (each a
miniature of its life area) around "Five parts of your life. One private file."
→ five chapters docking the real localized captures in glass devices beside
procedural demonstrations built from the demo data (budget bars 1100/470/120/
58/30, weight curve, macro rings 1270/2650, calendar, ranked entries) →
privacy: crystal settles into a glass phone → the world stays alive behind
how/storage/pricing/FAQ → crystal returns beside the final CTA.

## Tool contributions (honest record)
- Awesome Design.md: apple (depth by surface colour, one shadow on product
  plates only, functional backdrop-filter), tesla (frosted nav, 0.33s
  universal transition, no gradients on UI), expo (sky-blue wash behind hero
  on pure white) → the direction contract.
- Taste Skill: sans display (Instrument Serif dropped), ≤2-line headline,
  ≤20-word lead, no trust strip inside the hero (moved below), one accent,
  one radius system, no eyebrows/section numbers, hero split composition.
- img2threejs: its method (code-only primitives, staged blockout→material→
  lighting, agent-vision review of renders, comparison against the reference)
  drove the crystal, plates and demos; its forge scripts were NOT run — there
  is no reference photo to reconstruct, the objects are original geometry.
- Impeccable: `impeccable detect` (18 findings triaged: gradient text, layout
  transition, border+shadow doubles, spotlight glow fixed; em-dashes in legacy
  translated copy left) + finish-reviewer agent (two rounds; its eight fixes
  applied except screenshot-textured plates, replaced by procedural
  mini-screens because file:// image→WebGL would trip the gate's
  failed-request assertion).
- Playwright CLI: chromium installed; probe.js (13 scroll stops × desktop/
  mobile/RM with scene-state readout), resilience.js (WebGL failure, context
  loss/restore, tab order, contrast ≥4.98:1, 25 s scroll recording
  scroll-1280.webm), turnprobe.js; plus the repo's own landing_avail (13/13),
  landing_finish (320/390/768/1440 × 8 languages + no-JS) and green.js.

## Known limits
- Software WebGL (Playwright) renders glass flatter than a GPU; judge glass
  in Opera.
- Legacy sentences in FAQ/storage/pricing keep their em-dashes (retranslation
  ×7 out of scope).
- `#shotLightboxImg` has no src until opened (test-required element).

## Publish
Not published. Owner approves the exact SHA, then the repo's documented
deploy.js flow (`--check` → `--out` → commit to gh-pages → `--check` must say
byte-identical). Never push gh-pages directly.
