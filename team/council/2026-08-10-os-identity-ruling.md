# COUNCIL RULING — PRIVATE-OS IDENTITY (binding build spec)
**Chairman ruling · 2026-08-10 · pinned to tip `586909c`** (NOT `12ea9c6` — see "Who was wrong")
Supersedes nothing; extends `team/council/2026-08-10-landing-futurism-ruling.md`, whose NOT-built list (fullscreen menu, particles-over-text, sound) remains in force.

---

## VERDICT

Brief2's diagnosis is right and its prescription is wrong. The diagnosis — the page's mid-belt reads AI-built, the identity is "private operating system," the cursor-waked network is the signature, anti-cyberpunk discipline — is confirmed by both audits and adopted. The prescription — near-black page, "YOUR LIFE. ONE SYSTEM." hero, "ENTER MRLN" CTA, cursor-gated entry — is rejected on the merits, not merely on rank. The descent IS the brand argument: daylight = the life you can see, dark = the private system underneath it. A page that is black from the first pixel cannot dramatize the light dying where the privacy argument begins; it would spend MRLN's rarest move to buy a look a thousand crypto sites already own. All five seats and both audits converged here independently.

**The daylight-vs-near-black resolution:** the daylight order stands. Which surfaces carry the OS identity: the **boot** (first second), the **cursor** (continuous), the **dark privacy zone** (the climax, where the OS gets its *name*), and the **page-body type system** (ledger register in daylight). Which keep the daylight order: hero, hero copy, CTA, and everything above the descent. One-sentence version for Osefe's phone: **"The brief's black page would kill your own best move — the lights going out exactly where privacy begins only works if the page starts in daylight."** If Osefe reads that and still wants near-black, that is his order to reverse explicitly in-thread; we do not execute it off a third party's brief.

---

## WHAT SHIPS IN THE NEXT COMMIT (candidate A — tonight, one commit, zero i18n load)

**Owner: Kaito codes. Akashi reviews the 3 new strings against the true-facts rule. Hugo GREEN. No Mikoto load. No Arthur redline needed (no layout change).**

1. **Boot v3 finishing beats**, inside the existing IIFE (landing.html ~791–940), ~40 lines:
   - At `prog >= 0.96`: center canvas readout `PRIVATE SPACE / READY` (canvas designed-English; factual — restores the beat `586909c` deleted from the DOM, now in the boot's own grammar).
   - Fold-collapse → **~100ms full-black frame** → `MRLN // SYSTEM ONLINE` flash → existing fade. This mirrors index.html's own boot line (1506) — the identity handshake between landing and app, for free.
   - Fallback timer, session-skip, RM-skip, 2.2s cap, and the `586909c` listener cleanup all untouched. **The cap is a ceiling, not a floor — the boot does not get longer.**
2. **Marquee ticker → static mono system-status line** (or delete if it fights the hero). Template-tell #4; smallest kill, fits this commit.

## CANDIDATE B — this week (small, real Mikoto load, worth it)

**Owner: Kaito codes; Mikoto keys; Akashi/Hugo per pipeline.**

3. **The OS name goes in the dark zone, not the hero.** Oversized statement at the privacy dome, where the page is already near-black: **"Your private operating system."** — sentence case, serif, serious register; not all-caps shout. Cost: ~2 t-keys ×7. No title/og/meta churn. This is where brief2's line is *true*, and it makes the descent read as the brand sentence.

## CANDIDATE C — next week (the "one weird thing"; Arthur redlines FIRST, then Kaito builds)

4. **Privacy expressed through interaction** (brief2 item 3): `NO CLOUD / NO ACCOUNT / NO TRACKING` nodes in the dark zone that dissolve as the cursor approaches, reusing the boot's node/line/readout grammar — the boot's language bleeding into the page body is what stops the boot being a quarantined 2.2 seconds. Canvas-only, free-to-operate.
   **Binding condition (Contrarian's flag, upheld):** the canvas labels are garnish, not the claim. The same three claims must exist as static, translated DOM text in the section (the existing 7-language privacy copy + airplane-mode line at ~1085 stay adjacent and primary), so touch and reduced-motion users lose nothing. "Zero i18n cost" was true of the canvas, not of the section.
   Needs: pinned act, hit-testing, touch + RM static fallbacks. Full pipeline, its own gate candidate — **do not bundle with A or B.**

## WHAT WAITS (with triggers)

- **Template-kill wave in daylight** — features/how/storage card grids → system-ledger rows (mono uppercase labels, hairline rules, no boxes), boot dot-glyphs replacing emoji icon-wells, macOS browser-frame → MRLN's own chrome, section skeleton varied at least twice. **Trigger:** Arthur delivers redlines; Kaito implements; this is layout, so Arthur gates it. Sequenced after C so the ledger register can quote the boot's proven grammar.
- **Emoji → custom inline-SVG icon set** — highest cost on the table (icon design + re-keying every emoji-bearing string ×7; Mikoto's largest load this quarter). Not a register violation (CLAUDE.md permits functional icons) — premium polish, not a rule fix. **Trigger:** after the ledger-row redlines land, as its own candidate.
- **index.html eyebrow `// Personal Finance Command Center` → OS-era phrasing** — the one shipped string actively contradicting the identity (brief2 spotted it; brand audit confirmed at index 1950). One string, but it is app copy: Kaito drafts, Mikoto keys, normal pipeline. **Trigger:** bundle with candidate B or C, not alone.

## WHAT WE ARE NOT BUILDING — and why

1. **Near-black page-wide "discovered OS."** Reverses Osefe's hours-old explicit order, and the order is structurally correct: the descent needs daylight to die from. *Phone version: the black page would kill the lights-going-out move that is already your best idea.*
2. **Hero swap to "YOUR LIFE. ONE SYSTEM."** "App" names the purchasable category on a Stripe page; all-caps breaks the serious register; real cost is ~5 surfaces (title/og/twitter/hero-h1 ×7/closing h2 ×7), not 7 keys. The line ships anyway — in the dark zone (candidate B), where it is true. *Phone version: the system line is moving to the dark section where it's true, instead of replacing the sentence that tells buyers what they're buying.*
3. **CTA "ENTER MRLN."** Verified at lines 965–966: "Open MRLN" (free app) sits one button from "Get access" (paid, #pricing). ENTER promises entry and delivers a paywall, beside a button that actually enters free — two enter-verbs, opposite prices, at the decision point. *Phone version: ENTER MRLN would sit next to Open MRLN — one paid, one free — and people would click the wrong one.*
4. **"SYSTEM OFFLINE · MOVE YOUR CURSOR TO BEGIN" / click-to-wake gating.** Contradicts brief1's own hard rules ("Never block the actual page," ~2s max, lines 171–172); no cursor exists on the mobile traffic Maki is chasing; a discovery ritual in front of a price is bounce. ENTER-flavor lives in the boot transition (`SYSTEM → INITIALIZED`), untranslated micro-copy only.
5. **A longer or "more extreme" boot.** Every second taxes the least-invested visitor. The boot earns its keep by bleeding grammar into the page (candidate C), not by growing.
6. **"SYNCING" in any string, ever.** Akashi-blocked at `86364dc`, fixed at `586909c`. Binding precedent: **every boot/page readout must be a true product fact.** "SYSTEM OFFLINE" as a page state fails this too — the page arrived over the network.
7. **OS-chrome saturation** (status strings everywhere). Arthur's r5 precedent stands: a constant carries zero bits. OS *facts* stay; OS *costume* is cut.
8. Prior ruling's list — fullscreen menu, particles-over-text, sound — stays dead.

## WHO WAS WRONG (with the correct facts)

- **Brief2's author** — wrong on hero, CTA, and palette: researched mrln.online but never the funnel (ENTER→paywall collision, verified lines 965–966) and never Osefe's daylight order. Right — and adopted — on audit-first, anti-cyberpunk, the card-belt indictment, the cursor-as-signature, and the Command-Center eyebrow.
- **Brief1's author** — "SYNCING PERSONAL SPACE" (line 129) was a false claim about a product whose entire pitch is that nothing syncs; Akashi blocked it in production. The brief was never checked against the product's core promise.
- **The dispatch brief for this council (Kaito)** — pinned `12ea9c6` as tip. Wrong: the tip is **`586909c`** (Akashi's block at `86364dc`, fix at `586909c`). All sign-offs name `586909c` or later; a spec citing `12ea9c6` is already stale.
- **Contrarian** — claimed the sync-line fix "sits uncommitted in the shared worktree." Wrong: it is committed at `586909c`; the working tree is clean. The process alarm was measured against a stale snapshot.
- **Brand audit** — "zero i18n cost" on the dissolve-node labels was overstated: the canvas is free, but the claims those labels carry must exist as translated DOM or the section fails the 8-language and RM constraints (condition bound into candidate C).
- **Executor** — got the tip right when the dispatch didn't. Named because rule 4 cuts both ways.

## HONEST LIMITS

- This ruling is argument, not proof. Bounce/conversion claims about gating and near-black are informed judgment, not A/B data; if Osefe wants them tested rather than ruled, that is a legitimate ask — but the test is his call to fund, and the default stands meanwhile.
- Nothing here auto-publishes. Live gh-pages remains one commit behind and stays there until Osefe's explicit in-thread go. Every sign-off names its SHA; if the tip moves, the gate reopens.
- Kaito remains accountable for the outcome; this ruling is his spec, not his shield. It bows to `green.js` and a real device: if a candidate fails the suite, the suite wins.

**Build order: A (tonight) → B (this week) → C (next week, Arthur-gated) → template-kill wave → icon system.** Save this ruling to `team/council/` and reference it by SHA.